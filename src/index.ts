import dotenv from 'dotenv';

dotenv.config();

const start = async () => {
  // TODO: campaigns and metrics:
  // - Impressions
  // - Clicks
  // - Cost
  // - Conversions
  // - ConversionValue
  // - CTR
  // - CPC
  // - CPA
  // - ROAS
  // - ConversionRate
  debugger;
};

start().catch(e => {
  console.error(e);
});
